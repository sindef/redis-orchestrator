apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: redis-multisite-b
  labels:
    app: redis
    site: site-b
spec:
  serviceName: redis-headless
  replicas: 1
  selector:
    matchLabels:
      app: redis
      site: site-b
  template:
    metadata:
      labels:
        app: redis
        site: site-b
    spec:
      serviceAccountName: redis-orchestrator
      securityContext:
        fsGroup: 1000
      
      initContainers:
      # Configure Redis with password
      - name: redis-config
        image: redis:7-alpine
        command:
        - sh
        - -c
        - |
          cp /tmp/redis/redis.conf /data/redis.conf
          echo "masterauth ${REDIS_PASSWORD}" >> /data/redis.conf
          echo "requirepass ${REDIS_PASSWORD}" >> /data/redis.conf
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-password
              key: password
        volumeMounts:
        - name: redis-config
          mountPath: /tmp/redis
        - name: data
          mountPath: /data
      
      containers:
      # Redis container
      - name: redis
        image: redis:7-alpine
        command:
        - redis-server
        - /data/redis.conf
        ports:
        - containerPort: 6379
          name: redis
          protocol: TCP
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-password
              key: password
        volumeMounts:
        - name: data
          mountPath: /data
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 2000m
            memory: 2Gi
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - redis-cli -a "${REDIS_PASSWORD}" ping
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - redis-cli -a "${REDIS_PASSWORD}" ping
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      
      # Redis Orchestrator with Raft
      - name: orchestrator
        image: sindef/redis-orchestrator:v1.1.4
        imagePullPolicy: IfNotPresent
        args:
        # Redis connection
        - --redis-host=localhost
        - --redis-port=6379
        - --redis-service=redis
        - --label-selector=app=redis,site=site-b
        - --sync-interval=15s
        
        # Raft configuration - includes all peers across sites
        - --election-mode=raft
        - --raft-bind=0.0.0.0:7000
        - --raft-peers=redis-0.redis-headless.redis-multisite-a.svc.cluster.local:7000,redis-0.redis-headless.redis-multisite-b.svc.cluster.local:7000,redis-witness.redis-headless.redis-multisite-c.svc.cluster.local:7000
        - --raft-data-dir=/var/lib/raft
        - --debug=true
        
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-password
              key: password
        - name: SHARED_SECRET
          valueFrom:
            secretKeyRef:
              name: orchestrator-secret
              key: shared-secret
        
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 7000
          name: raft
          protocol: TCP
        
        volumeMounts:
        - name: raft-data
          mountPath: /var/lib/raft
        
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      
      volumes:
      - name: redis-config
        configMap:
          name: redis-config
  
  volumeClaimTemplates:
  # Redis data storage
  - metadata:
      name: data
      labels:
        app: redis
        site: site-b
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
  
  # Raft persistent storage
  - metadata:
      name: raft-data
      labels:
        app: redis-orchestrator
        site: site-b
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi

