# Optional: Secret for DragonflyDB password
# Only needed if you want authentication enabled

---
apiVersion: v1
kind: Secret
metadata:
  name: dragonfly-password
  namespace: default
  labels:
    app.kubernetes.io/name: dragonfly
    app.kubernetes.io/component: auth
type: Opaque
stringData:
  # CHANGE THIS PASSWORD!
  password: "changeme123"

